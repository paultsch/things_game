<h1>New submission</h1>

<%= form_for @submission do |f| %>
<div class="form-group">
  <%= f.label :round %>
  <%= f.text_field :round, class: 'form-control'  %>
</div>

<div class="form-group">
  <%= f.label :submission %>
  <%= f.text_area :submission, class: 'form-control'  %>
</div>

<div class="form-group">
  <%= f.submit class: 'btn btn-primary' %>
</div>

<%= link_to "Back to Lobby", game_lobby_path, class: 'btn btn-success' %>

<% end %>

<p>need to work on the create action to have it submit round and submission to database and redirect user back to game lobby. Needs to submit current user id, round (passed via params), game_code (passwed via params). </p>
